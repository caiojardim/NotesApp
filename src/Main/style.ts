import styled from 'styled-components'

export const Container = styled.div`
    color: #008;
    textarea { 
        resize: none;
        width: 80%;
        margin: 0 auto;
        display: flex;
    }
`